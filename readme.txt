This project is built with deno, therefore there is no need to clone this repository;

Run
    deno run --unstable --allow-all https://raw.githubusercontent.com/not-ivy/mp-discord/master/main.ts
to start the rich presence.

Problems to fix:
  - Crashes if no music is playing;
  - Crashes if discord is not running;

Features to add:
  - Timestamp when music end;
  - Small icon;
  - Hover text;